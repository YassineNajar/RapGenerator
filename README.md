# 🎤 Rap Generator using Local LLM

This is a simple Streamlit app that generates rap lyrics using a locally hosted LLM (e.g., Mistral via Ollama).

## 🚀 Features

- Generate rap lyrics based on topic, mood, length, and style
- Streamlit interface for user input
- Uses local LLM (tested with Ollama + Mistral)

## 🔧 Requirements

- Python 3.9+
- Streamlit
- `requests` library
- Ollama running locally with a model like `mistral`

## 🛠 Installation

```bash
pip install streamlit requests
